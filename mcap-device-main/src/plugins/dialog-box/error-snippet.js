/**
 * $root/package.yaml 
 * ERROR snippet
 */

export default `# необходим добавить параметры в $root/package.yaml

dialog-box:
  #delay: 60
  #alpha: 0.3
  #endPoint: true
  mode-classic: dialog-box # изображение 1024 x 200
  mode-fullscreen: dialog-box-full # изображение 1024 x 768` 