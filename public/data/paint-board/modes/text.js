/*
class Text {
  constructor (x, y, width, height, color, id=null){
    this.id = id
    this.x = x
    this.y = y
    this.width = width
    this.height = height
    this.color = color
  }
  render (ctx){
    ctx.save()
    ctx.beginPath()
    ctx.strokeStyle = this.color
    ctx.strokeRect(this.x, this.y, this.width, this.height)
    ctx.closePath() 
    ctx.restore()
  }
}


export default function (point_1, point_2){
    let width = point_2.x-point_1.x
    let height =  point_2.y-point_1.y
    this.draw(new Text(point_1.x, point_1.y, width, height, this.color))
    this.update()
}
*/