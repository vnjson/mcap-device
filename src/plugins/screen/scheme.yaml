# добавляем возможность одноразового клика на весь экран
# При клике срабатывает плагин [ next ] 
- screen:
    onClick: true

- dialog-box: false
  screen:
    onClick: true

# после клика возмозность клика по экрану отключается
# Если у экрана стоит параметр onClick: true и при этом присутсвует возможность
# клика по dialog-box то плагин [ next ] отпработает дважды
# 



# Экран это div элемент с короторый находится на первом уровне внутри элемента #screen . У элемента экрана есть id и класс .screen

#<div id="screen">
#  <div id="screenName" class="screen">
#    Первый экран
#  </div>
#</div>

# stream - является основный экраном, где происходит прохождение новеллы

# Размеры экрана шираина 1024px высота: 768px