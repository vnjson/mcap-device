  /*
  animate(_keyframes, _options) {
      const keyframes = [
          { transform: 'translateX(0%)' },
          { transform: 'translateX(5%)' },
          { transform: 'translateX(0%)' },
          { transform: 'translateX(-5%)' },
          { transform: 'translateX(0%)' },
      ];
      const options = {
          // timing options
          duration: 1000,
          iterations: 3, // Infinity
      };
      const animation = this.root.animate(keyframes, options);
      animation.addEventListener("finish", () => {
         // vnjs.emit(`${this.name}.animation-end`);
          // onEnd: {ctx}
      });*/
    }
    //swing() {}
    //snake() {}


/*
animate:
    target: menu
    # type: blink # snake
    duration: 1000
    repeat: 0
    steps:
      - opacity: 0.4
        height: 400px
      - opacity: 0.5
*/